<div class = "flex flex-col items-center gap-4 pb-10">
  <div class="flex flex-col">
    <h1 class = "text-2xl md:text-6xl my-4 p-2 text-center bg-[#50AE26] text-white w-1/2 place-self-center" >Adjusted Stats</h1>
    <p class = "text-l md:text-xl text-center p-1 w-1/2 place-self-center">This page allows you to see the top 25 seasons for OPS+ and SLG+.
    OPS+ and SLG+ are adjusted stats, meaning they compare a players performance in one season against the rest of the leagues performance during that season.
    The effective difference here is that OPS+ includes batting average, while SLG+ removes batting average and focuses only on power.
    Only players with 20 or more atbats are eligible</h2>
    </div>
  <div class="flex flex-col md:flex-row gap-10 flex-1">
    <div class="h-full">
      <table class ="w-96 bg-white tablesorter shadow rounded border-b border-gray-200">
        <thead class = "bg-[#50AE26] text-white">
          <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Rank</th>
          <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Team</th>
          <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Name</th>
          <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Year</th>
          <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">OPS+</th>
        <thead>
        <tbody class = "text-gray-700">
          <%@singleopsPlus.each_with_index do |player, index|%>
            <tr class="hover:bg-gray-200">
              <td class="text-left py-1 px-2"><%=index + 1%></td>
              <td class="text-left py-1 px-2"><%=link_to player.player.name, player_path(player.player)%></td>
              <td class="text-left py-1 px-2"><%=player.team.team_name%></td>
              <td class="text-left py-1 px-2"><%=player.years%></td>
              <td class="text-left py-1 px-2"><%=number_with_precision(player.opsPlus, precision: 0)%></td>
            </tr>
          <%end%>
        </tbody>
      </table>
    </div>
  <div class="h-full">
  <table class ="w-96 bg-white shadow rounded border-b border-gray-200">
    <thead class = "bg-[#50AE26] text-white">
    <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Rank</th>
    <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Name</th>
    <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Team</th>
    <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">Year</th>
    <th class = "text-left py-3 px-2 uppercase font-semibold text-sm hover:cursor-pointer">SLG+</th>
    <thead>
    <tbody class = "text-gray-700">
    <%@singleslgPlus.each_with_index do |player, index|%>
        <tr class="hover:bg-gray-200">
        <td class="text-left py-1 px-2"><%=index + 1%></td>
        <td class="text-left py-1 px-2"><%=link_to player.player.name, player_path(player.player)%></td>
        <td class="text-left py-1 px-2"><%=player.team.team_name%></td>
        <td class="text-left py-1 px-2"><%=player.years%></td>
        <td class="text-left py-1 px-2"><%=number_with_precision(player.slgPlus, precision: 0)%></td>
    </tr>
    <%end%>
    <tbody>
  </table>
</div>
</div>
</div>